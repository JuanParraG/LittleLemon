import logo from "../sources/Logo.svg"

function Header(){
    return(
        <header>
             <img src={logo} alt="Yellow Lemon. Little lemon's logo."></img>
        </header>
    )
}
export default Header;