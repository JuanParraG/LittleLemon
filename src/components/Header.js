import logo from "../sources/Logo.svg"

function Header(){
    return(
        <header>
             <img src={logo} alt="Yellow Lemon. Little lemon's logo."/>
        </header>
    )
}
export default Header;